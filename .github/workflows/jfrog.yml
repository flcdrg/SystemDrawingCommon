name: jfrog

on:
  workflow_dispatch:

jobs:
  scan:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Frogbot by JFrog
        uses: jfrog/frogbot@v2.21.9
          
